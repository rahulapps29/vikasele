
sudo rm -f /etc/nginx/sites-enabled/default
sudo rm -f /etc/nginx/sites-available/default
sudo rm -f /etc/nginx/conf.d/default.conf


rm /etc/nginx/sites-enabled/default



nano /etc/nginx/sites-available/vikaselectricals.shop




then 

server {
    listen 80;
    server_name vikaselectricals.shop www.vikaselectricals.shop;

    location / {
        proxy_pass http://localhost:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo ln -s /etc/nginx/sites-available/vikaselectricals.shop /etc/nginx/sites-enabled/




apt install certbot python3-certbot-nginx -y
certbot --nginx -d vikaselectricals.shop -d www.vikaselectricals.shop
certbot renew --dry-run



ln -s /etc/nginx/sites-available/backend_ojasadvisors.com /etc/nginx/sites-enabled/

nano /etc/nginx/sites-available/ojasadvisors.com
server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name ojasadvisors.com www.ojasadvisors.com; 

 
    location / {
        proxy_pass http://localhost:4072;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}


server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name back.ojasadvisors.com;  # Server name or domain name this block will respond to

 location / {
        proxy_pass http://localhost:4073;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}






sudo nano /etc/nginx/sites-available/frontend_gatelockorbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name lock.orbe.in;  

 location / {
        proxy_pass http://localhost:4076;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/frontend_gatelockorbe.in /etc/nginx/sites-enabled/






sudo nano /etc/nginx/sites-available/rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name rahulluthra.in www.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4011;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/rahulluthra.in
sudo ln -s /etc/nginx/sites-available/rahulluthra.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
pm2 start npm --name rahulluthra_build -- start
pm2 save


sudo nano /etc/nginx/sites-available/utilitybackend_rahulluthra.in
server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name utility.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4028;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/utilitybackend_rahulluthra.in
sudo ln -s /etc/nginx/sites-available/utilitybackend_rahulluthra.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/



sudo certbot --nginx -d utility.rahulluthra.in 
pm2 start npm --name rahulluthra_utility_build -- start
pm2 save



sudo nano /etc/nginx/sites-available/ledger_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name ledger.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4017;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/ledger_rahulluthra.in
sudo ln -s /etc/nginx/sites-available/ledger_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d ledger.rahulluthra.in 
pm2 start npm --name rahulluthra_ledger_build -- start
pm2 save

sudo nano /etc/nginx/sites-available/records_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name records.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4018;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/records_rahulluthra.in
sudo ln -s /etc/nginx/sites-available/records_rahulluthra.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d records.rahulluthra.in 
sudo ufw allow 4018
pm2 start npm --name rahulluthra_records_build -- start
pm2 save


sudo nano /etc/nginx/sites-available/dash_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name dash.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4019;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/dash_rahulluthra.in
sudo ln -s /etc/nginx/sites-available/dash_rahulluthra.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d dash.rahulluthra.in 
sudo ufw allow 4019
pm2 start npm --name rahulluthra_dash_build -- start
pm2 save

sudo nano /etc/nginx/sites-available/report_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name report.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4054;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/report_rahulluthra.in
sudo ln -s /etc/nginx/sites-available/report_rahulluthra.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo ufw allow 4054
pm2 start npm --name report_rahulluthra -- start
pm2 save



sudo nano /etc/nginx/sites-available/backend_gatelockorbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name lockbackend.orbe.in;  

 location / {
        proxy_pass http://localhost:4075;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/backend_gatelockorbe.in
sudo ln -s /etc/nginx/sites-available/backend_gatelockorbe.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo ufw allow 4075
sudo certbot --nginx -d lockbackend.orbe.in
pm2 start npm --name lock_controller -- start
pm2 save


sudo nano /etc/nginx/sites-available/survana.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name survana.in www.survana.in; 

 
    location / {
        proxy_pass http://localhost:4077;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo rm /etc/nginx/sites-enabled/survana.in
sudo ln -s /etc/nginx/sites-available/survana.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d survana.in -d www.survana.in
sudo ufw allow 4077
pm2 start npm --name survana_build -- start
pm2 save



sudo nano /etc/nginx/sites-available/backend_survana.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name video.survana.in;

 
    location / {
        proxy_pass http://localhost:4041;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}


sudo rm /etc/nginx/sites-enabled/backend_survana.in
sudo ln -s /etc/nginx/sites-available/backend_survana.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d video.survana.in
sudo ufw allow 4041
pm2 start npm --name backend_survana -- start
pm2 save



sudo nano /etc/nginx/sites-available/rrpchambers_prupley.com
server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name rrpchambers.prupley.com;  

 location / {
        proxy_pass http://localhost:4083;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/rrpchambers_prupley.com
sudo ln -s /etc/nginx/sites-available/rrpchambers_prupley.com /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rrpchambers.prupley.com
sudo ufw allow 4083
pm2 start npm --name rrpchambersprupleybuild -- start
pm2 save


sudo nano /etc/nginx/sites-available/orbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name www.orbe.in orbe.in;  

 location / {
        proxy_pass http://localhost:4048;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo rm /etc/nginx/sites-enabled/orbe.in
sudo ln -s /etc/nginx/sites-available/orbe.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d orbe.in -d www.orbe.in
sudo ufw allow 4048
pm2 start npm --name orbebuild -- start
pm2 save



sudo nano /etc/nginx/sites-available/drive_orbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name drive.orbe.in; 

 location / {
        proxy_pass http://localhost:4039;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/drive_orbe.in
sudo ln -s /etc/nginx/sites-available/drive_orbe.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d drive.orbe.in
sudo ufw allow 4039
pm2 start npm --name orbedrivebuild -- start
pm2 save


sudo nano /etc/nginx/sites-available/algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name algoapp.in www.algoapp.in;  

 location / {
        proxy_pass http://localhost:4046;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d algoapp.in  -d www.algoapp.in


sudo nano /etc/nginx/sites-available/photos_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name photos.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4021;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/photos_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
sudo certbot --nginx -d backend.rahulluthra.in 
sudo certbot --nginx -d utility.rahulluthra.in 
sudo certbot --nginx -d photos.rahulluthra.in 






sudo nano /etc/nginx/sites-available/echolens_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name echolens.algoapp.in;  

 location / {
        proxy_pass http://localhost:4052;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/echolens_algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d echolens.algoapp.in


sudo nano /etc/nginx/sites-available/flipkart_frontend_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name flipkart.algoapp.in;  

 location / {
        proxy_pass http://localhost:4042;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/flipkart_frontend_algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d flipkart.algoapp.in


sudo nano /etc/nginx/sites-available/flipkart_backend_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name flipkartb.algoapp.in;  

 location / {
        proxy_pass http://localhost:4043;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/flipkart_backend_algoapp.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d flipkartb.algoapp.in
pm2 start npm --name algoapp_flipkart_backend -- start




sudo nano /etc/nginx/sites-available/amanproperties.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name www.amanproperties.in amanproperties.in;  

 location / {
        proxy_pass http://192.168.1.197:4082;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/amanproperties.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d amanproperties.in -d www.amanproperties.in


sudo nano /etc/nginx/sites-available/backend_amanproperties.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name backend.amanproperties.in;  

 location / {
        proxy_pass http://192.168.1.197:4081;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/backend_amanproperties.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d backend.amanproperties.in



sudo nano /etc/nginx/sites-available/eduquant_prupley.com

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name eduquant.prupley.com;  

 location / {
        proxy_pass http://localhost:4082;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/eduquant_prupley.com /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d eduquant.prupley.com



sudo nano /etc/nginx/sites-available/eduquant.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name www.eduquant.in eduquant.in;  

 location / {
        proxy_pass http://localhost:4062;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/eduquant.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-available/

sudo certbot --nginx -d eduquant.in -d www.eduquant.in
pm2 start npm --name eduquantbuild -- start


sudo nano /etc/nginx/sites-available/dalipsugarapp_trackwellness.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name dalip.trackwellness.in;  

 location / {
        proxy_pass http://localhost:3995;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/dalipsugarapp_trackwellness.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d dalip.trackwellness.in
pm2 start npm --name dalipsugarapp_trackwellness -- start
pm2 save


sudo ufw allow 4005
sudo nano /etc/nginx/sites-available/dalipsugarreport_trackwellness.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name report.trackwellness.in;  

 location / {
        proxy_pass http://localhost:4005;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/dalipsugarreport_trackwellness.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d report.trackwellness.in
pm2 start npm --name dalipsugarreport_trackwellness -- start
pm2 save














sudo nano /etc/nginx/sites-available/prupley.com

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name prupley.com www.prupley.com;  

 location / {
        proxy_pass http://localhost:4039;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo rm /etc/nginx/sites-enabled/prupley.com
sudo ln -s /etc/nginx/sites-available/prupley.com /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d prupley.com  -d www.prupley.com
pm2 start npm --name prupleybuild -- start
pm2 save



sudo nano /etc/nginx/sites-available/frontend_sslegal.in


server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name sslegal.prupley.com;  

 location / {
        proxy_pass http://localhost:4074;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/frontend_sslegal.in /etc/nginx/sites-enabled/


sudo certbot --nginx -d sslegal.pruply.com
pm2 start npm --name sslegalbuild -- start
pm2 save


sudo nano /etc/nginx/sites-available/mailadmin_prupley.in


server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name mailadmin.prupley.com;  

 location / {
        proxy_pass http://localhost:4174;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo rm /etc/nginx/sites-enabled/mailadmin_prupley.in
sudo ln -s /etc/nginx/sites-available/mailadmin_prupley.in /etc/nginx/sites-enabled/


sudo certbot --nginx -d mailadmin.prupley.com
pm2 start npm --name mailadminprupley -- start
pm2 save



localfile upload to server

#!/bin/bash
rsync -avz -e "ssh -p 22" \
  --exclude "node_modules" \
  ~/Documents/webserver/orbe/ \
  rahul@161.97.80.191:~/webserver/orbe/
 


  --exclude ".git" \