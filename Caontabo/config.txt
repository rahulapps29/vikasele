
rm /etc/nginx/sites-enabled/default



nano /etc/nginx/sites-available/vikaselectricals.shop




then 

server {
    listen 80;
    server_name vikaselectricals.shop www.vikaselectricals.shop;

    location / {
        proxy_pass http://localhost:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/vikaselectricals.shop /etc/nginx/sites-enabled/
sudo ln -s /etc/nginx/sites-available/survana.in /etc/nginx/sites-enabled/

sudo nano /etc/nginx/sites-available/survana.in
server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name survana.in www.survana.in; 

 
    location / {
        proxy_pass http://localhost:4077;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

certbot --nginx -d video.survana.in
certbot --nginx -d survana.in -d www.survana.in
ls -l /etc/nginx/sites-enabled/


apt install certbot python3-certbot-nginx -y
certbot --nginx -d vikaselectricals.shop -d www.vikaselectricals.shop
certbot renew --dry-run



ln -s /etc/nginx/sites-available/backend_ojasadvisors.com /etc/nginx/sites-enabled/

nano /etc/nginx/sites-available/ojasadvisors.com
server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name ojasadvisors.com www.ojasadvisors.com; 

 
    location / {
        proxy_pass http://localhost:4072;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}


server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name back.ojasadvisors.com;  # Server name or domain name this block will respond to

 location / {
        proxy_pass http://localhost:4073;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo nano /etc/nginx/sites-available/backend_gatelockorbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name lockbackend.orbe.in;  

 location / {
        proxy_pass http://localhost:4075;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/backend_gatelockorbe.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d lockbackend.orbe.in
sudo certbot --nginx -d lock.orbe.in
sudo nano /etc/nginx/sites-available/frontend_gatelockorbe.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name lock.orbe.in;  

 location / {
        proxy_pass http://localhost:4076;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/frontend_gatelockorbe.in /etc/nginx/sites-enabled/



sudo nano /etc/nginx/sites-available/frontend_sslegal.in


server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name sslegal.prupley.com;  

 location / {
        proxy_pass http://localhost:4074;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

ln -s /etc/nginx/sites-available/frontend_sslegal.in /etc/nginx/sites-enabled/


sudo certbot --nginx -d sslegal.pruplay.com



sudo nano /etc/nginx/sites-available/rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name rahulluthra.in www.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4011;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
ln -s /etc/nginx/sites-available/rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 


sudo nano /etc/nginx/sites-available/backend_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name backend.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4029;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/backend_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
sudo certbot --nginx -d backend.rahulluthra.in 



sudo nano /etc/nginx/sites-available/utilitybackend_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name utility.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4028;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/utilitybackend_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
sudo certbot --nginx -d backend.rahulluthra.in 
sudo certbot --nginx -d utility.rahulluthra.in 





sudo nano /etc/nginx/sites-available/prupley.com

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name prupley.com www.prupley.com;  

 location / {
        proxy_pass http://localhost:4079;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
ln -s /etc/nginx/sites-available/prupley.com /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d prupley.com  -d www.prupley.com




sudo nano /etc/nginx/sites-available/algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name algoapp.in www.algoapp.in;  

 location / {
        proxy_pass http://localhost:4046;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d algoapp.in  -d www.algoapp.in


sudo nano /etc/nginx/sites-available/photos_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name photos.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4021;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/photos_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
sudo certbot --nginx -d backend.rahulluthra.in 
sudo certbot --nginx -d utility.rahulluthra.in 
sudo certbot --nginx -d photos.rahulluthra.in 


sudo nano /etc/nginx/sites-available/ledger_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name ledger.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4017;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/ledger_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d rahulluthra.in  -d www.rahulluthra.in 
sudo certbot --nginx -d backend.rahulluthra.in 
sudo certbot --nginx -d utility.rahulluthra.in 
sudo certbot --nginx -d ledger.rahulluthra.in 




sudo nano /etc/nginx/sites-available/echolens_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name echolens.algoapp.in;  

 location / {
        proxy_pass http://localhost:4052;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/echolens_algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d echolens.algoapp.in


sudo nano /etc/nginx/sites-available/flipkart_frontend_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name flipkart.algoapp.in;  

 location / {
        proxy_pass http://localhost:4042;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/flipkart_frontend_algoapp.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d flipkart.algoapp.in


sudo nano /etc/nginx/sites-available/flipkart_backend_algoapp.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name flipkartb.algoapp.in;  

 location / {
        proxy_pass http://localhost:4043;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/flipkart_backend_algoapp.in /etc/nginx/sites-enabled/
sudo ls -l /etc/nginx/sites-enabled/
sudo certbot --nginx -d flipkartb.algoapp.in
pm2 start npm --name algoapp_flipkart_backend -- start


sudo nano /etc/nginx/sites-available/records_rahulluthra.in

server {
 listen 80;  # Listen on port 80 for incoming HTTP requests
 server_name records.rahulluthra.in;  

 location / {
        proxy_pass http://localhost:4018;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
sudo ln -s /etc/nginx/sites-available/records_rahulluthra.in /etc/nginx/sites-enabled/

sudo ls -l /etc/nginx/sites-enabled/

sudo certbot --nginx -d records.rahulluthra.in 

